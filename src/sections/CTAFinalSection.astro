---
// src/sections/CTAFinalSection.astro
// Bloque de cierre. Fondo uva con gran titular y botón principal.
// TIPOGRAFÍA: titular con mixed weight — el clímax tipográfico de la página.
---

<section class="cta-final" id="contacto">
  <div class="cta-final__inner">

    <!-- Número de sección -->
    <p class="section-num-light" aria-hidden="true">07</p>

    <!--
      GRAN TITULAR MIXED WEIGHT — clímax de la página.
      "Tu web puede"        → 400, casi se susurra
      "sentirse diferente." → 900 itálica, lima, máximo peso
      
      Es el mayor uso de mixed weight en toda la web:
      el tamaño enorme (hasta 8rem) hace que el contraste
      400 vs 900 sea absolutamente visible y dramático.
    -->
    <h2 class="cta-final__title">
      <span class="cta-final__title-thin">Tu web puede</span>
      <em class="cta-final__title-heavy">sentirse diferente.</em>
    </h2>

    <!-- Texto -->
    <p class="cta-final__text">
      Si quieres una página que conecte contigo y con tu audiencia
      desde el primer segundo, hablemos y vemos si encajamos.
    </p>

    <!-- CTA -->
    <div class="cta-final__actions">
      <a href="/contacto" class="cta-final__btn">
        Trabajemos juntos
      </a>
      <p class="cta-final__micro">Available worldwide. Based in Spain.</p>
    </div>

  </div>

  <!-- Decoración: círculos concéntricos -->
  <div class="cta-final__rings" aria-hidden="true">
    <span></span><span></span><span></span>
  </div>
</section>

<!-- Footer mínimo -->
<footer class="footer">
  <div class="footer__inner">
    <p class="footer__brand">AndreaFusion</p>
    <nav class="footer__nav" aria-label="Navegación footer">
      <a href="/portfolio">Portfolio</a>
      <a href="/sobre-mi">Sobre mí</a>
      <a href="/contacto">Contacto</a>
    </nav>
    <p class="footer__copy">© 2025 AndreaFusion. Todos los derechos reservados.</p>
  </div>
</footer>

<style>
  /* ── CTA Final ── */
  .cta-final {
    position: relative;
    z-index: 17;
    background: var(--color-bg-hero);
    overflow: hidden;
    padding-bottom: 0;
    will-change: transform;
  }

  .cta-final__inner {
    position: relative;
    z-index: 2;
    max-width: 900px;
    margin: 0 auto;
    padding: 7rem 1.5rem 6rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 2rem;
  }

  .section-num-light {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.25em;
    color: var(--color-accent-pink);
    text-transform: uppercase;
  }

  /* ── TITULAR MIXED WEIGHT — máximo impacto ── */
  .cta-final__title {
    display: flex;
    flex-direction: column;
    font-family: var(--font-display);
    font-size: clamp(3rem, 10vw, 8rem);
    line-height: 0.95;
    letter-spacing: -0.03em;
    gap: 0.05em;
  }

  /*
    "Tu web puede" — 400, crema muy tenue.
    A este tamaño enorme, el 400 parece casi transparente.
    El ojo lo lee como preparación, no como afirmación.
  */
  .cta-final__title-thin {
    font-weight: 400;
    font-style: normal;
    color: rgba(245, 238, 230, 0.45); /* crema casi fantasma */
    display: block;
  }

  /*
    "sentirse diferente." — 900 itálica, lima.
    El mayor contraste de la página: tamaño máximo + peso máximo + color acento.
    Es el momento visual más alto de todo el scroll.
  */
  .cta-final__title-heavy {
    font-weight: 900;
    font-style: italic;
    color: var(--color-accent-lime);
    display: block;
  }

  /* ── Texto ── */
  .cta-final__text {
    font-family: var(--font-body);
    font-size: clamp(1rem, 2.5vw, 1.15rem);
    line-height: 1.7;
    color: rgba(245, 238, 230, 0.7);
    max-width: 44ch;
  }

  /* ── Acciones ── */
  .cta-final__actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.5rem;
  }

  .cta-final__btn {
    display: inline-flex;
    align-items: center;
    padding: 1.1em 2.8em;
    background: var(--color-accent-lime);
    color: var(--color-text-dark);
    font-family: var(--font-body);
    font-size: clamp(1rem, 2.5vw, 1.15rem);
    font-weight: 700;
    border-radius: 9999px;
    box-shadow: 0 6px 28px rgba(200, 241, 53, 0.4);
    transition: transform 0.18s ease, box-shadow 0.18s ease;
  }

  .cta-final__btn:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 36px rgba(200, 241, 53, 0.5);
  }

  .cta-final__micro {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-style: italic;
    letter-spacing: 0.05em;
    color: rgba(245, 238, 230, 0.4);
  }

  /* ── Anillos decorativos ── */
  .cta-final__rings {
    position: absolute;
    bottom: -8rem;
    left: 50%;
    transform: translateX(-50%);
    pointer-events: none;
    z-index: 1;
  }

  .cta-final__rings span {
    position: absolute;
    border-radius: 50%;
    border: 1px solid rgba(200, 241, 53, 0.08);
    transform: translate(-50%, -50%);
  }

  .cta-final__rings span:nth-child(1) { width: 300px; height: 300px; }
  .cta-final__rings span:nth-child(2) { width: 500px; height: 500px; }
  .cta-final__rings span:nth-child(3) { width: 700px; height: 700px; }

  /* ── Footer ── */
  .footer {
    background: var(--color-text-dark);
    color: rgba(245, 238, 230, 0.5);
    position: relative;
    z-index: 17;
  }

  .footer__inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    align-items: center;
    text-align: center;
  }

  .footer__brand {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 1.1rem;
    color: var(--color-text-hero);
    letter-spacing: -0.01em;
  }

  .footer__nav {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .footer__nav a {
    font-family: var(--font-body);
    font-size: 0.82rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: rgba(245, 238, 230, 0.5);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer__nav a:hover { color: var(--color-accent-lime); }

  .footer__copy {
    font-family: var(--font-body);
    font-size: 0.75rem;
    letter-spacing: 0.03em;
  }

  /* ── Desktop ── */
  @media (min-width: 768px) {
    .footer__inner {
      flex-direction: row;
      justify-content: space-between;
      text-align: left;
    }
    .cta-final__inner { padding: 9rem 4rem 8rem; }
  }
</style>